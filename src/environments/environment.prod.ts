// Replace REPLACE_WITH_TURNSTILE_SITE_KEY with your real site key from
// https://dash.cloudflare.com/ → Turnstile → Add Site
export const environment = {
  production: true,
  turnstileSiteKey: 'REPLACE_WITH_TURNSTILE_SITE_KEY',
};
